rust
===

```bash
cargo new --bin <name>
cd <name>
cargo run
cargo build --verbose
cargo clean
```

### build for lib
```bash
cargo build --crate-type lib
```

### build for bin
```bash
cargo build --crate-type bin --release
```
